# backend/face_auth.py
# Skeleton: use OpenCV + dlib/face_recognition depending on availability.
import os
def authenticate_face(frame):
    # Return True/False. Real implementation: compare frame to images in authorized_faces/
    # Placeholder to keep code runnable.
    return False
